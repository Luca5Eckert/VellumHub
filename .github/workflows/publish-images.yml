name: Publish validated images

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  verify-and-publish:
    uses: ./.github/workflows/verify-images.yml
    with:
      publish: true
